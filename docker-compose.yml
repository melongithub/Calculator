version: '3.8'
services:
  my-web-app:
    build: .             # 告诉 Portainer：不要直接用原版镜像了，用我的 Dockerfile 打包一个新的！
    pull_policy: build   # 核心参数：保证每次你改了 GitHub 代码，它都会重新打包最新代码
    ports:
      - "8080:80"  
    restart: always
